# [[GANs]]

Generative Adversarial Networks ([[GANs]]) are a class of artificial intelligence algorithms used in unsupervised machine learning. They consist of two neural networks, a generator and a discriminator, competing against each other. [[GANs]] are primarily used to generate new data instances that resemble the training data, such as images, music, or text.
