# [[Quantization]]

[[Quantization]] is the process of mapping input values from a large set (often a continuous set) to output values in a smaller set (often a discrete set). In digital [[signal-processing]], after a [[signal-processing]] is [[sampling]], its amplitude values are [[quantization]], meaning they are approximated to a finite set of discrete levels. This introduces [[quantization]] error or noise.
